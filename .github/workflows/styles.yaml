# Do not edit this file! Make a pull request on changing
# github/workflows/drupal/styles.yaml in
# https://github.com/itk-dev/devops_itkdev-docker if need be.

### ### Drupal Styles (CSS and SCSS)
###
### Validates styles files.
###
### #### Assumptions
###
### 1. A docker compose service named `prettier` for running
###    [Prettier](https://prettier.io/) exists.

name: Styles

on:
  pull_request:
    paths: &paths
      - "web/themes/custom/**/css/**/*.css"
      - "web/themes/custom/**/css/**/*.scss"
      - "docker-compose.yml"
  push:
    branches:
      - main
      - develop
    paths: *paths

jobs:
  styles-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Create docker network
        run: |
          docker network create frontend

      - run: |
          docker compose run --rm prettier 'web/themes/custom/**/css/**/*.{css,scss}' --check
